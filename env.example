# Memoora Calls Microservice Environment Variables

# Server Configuration
PORT=5005
NODE_ENV=development

# Base URL for webhooks (use ngrok URL for local testing)
BASE_URL=http://localhost:5005

# Allowed domains for CORS (comma-separated)
ALLOWED_DOMAINS=localhost,127.0.0.1

# Twilio Configuration
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890

# Alpha Sender ID Configuration (for professional caller ID)
# Set to 'true' to enable alpha sender ID (shows "Memoora" instead of phone number)
# NOTE: Requires special Twilio account setup - see ALPHA_SENDER_TWILIO_SETUP.md
USE_ALPHA_SENDER_ID=false
# The text name to display as caller ID (max 11 characters)
ALPHA_SENDER_ID=Memoora
# Fallback phone number if alpha sender ID fails (optional, defaults to TWILIO_PHONE_NUMBER)
FALLBACK_PHONE_NUMBER=+1234567890

# Optional: Logging
LOG_LEVEL=info

# Optional: Test phone number for alpha sender ID testing
TEST_PHONE_NUMBER=+1234567890

# Main Backend URL for webhook notifications
MAIN_BACKEND_URL=https://memoora-backend.onrender.com
