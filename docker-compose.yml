version: '3.8'

services:
  memoora-calls:
    build: .
    ports:
      - "5005:5005"
    environment:
      - NODE_ENV=development
      - PORT=5005
    volumes:
      - ./recordings:/app/recordings
      - ./.env:/app/.env
    restart: unless-stopped
